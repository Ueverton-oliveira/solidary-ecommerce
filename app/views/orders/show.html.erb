<h1 class="h3 mb-4 text-gray-800">Detalhes do Pedido</h1>

<div class="row">
  <div class="col-lg-12">
    <div class="card shadow mb-4">
      <div class="card-body">
        <%= render @order %>
      </div>

      <div>
        <%= link_to "Editar pedido", edit_order_path(@order) %> |
        <%= link_to "Voltar para pedidos", orders_path %>

      </div>
    </div>
  </div>
</div>